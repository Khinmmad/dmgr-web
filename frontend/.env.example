# Base URL of the Go backend (releases API + download counter).
# Leave empty for a fully static build (bundled fallback links, counters hidden).
# Local dev example:
PUBLIC_API_BASE=http://localhost:8080
